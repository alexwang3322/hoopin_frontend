# Loaded automatically by `vite build` (production mode) — this is what
# .github/workflows/deploy.yml's CI build bakes into the deployed bundle.
# Not a secret; the deployed backend URL is public. See .env.example for the
# local-dev override (.env.local, gitignored).
VITE_API_BASE_URL=https://hoopin-backend.alex-wang3322.workers.dev/v1

# Clerk Dashboard -> API Keys -> Publishable key.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_c2Vuc2libGUtZ2Vja28tMjUzNy5jbGVyay5hY2NvdW50cy5kZXYk